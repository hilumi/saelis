/**
 * Application version — a development-safe constant (not read from
 * package.json at runtime, which is fragile under bundling).
 * Update alongside CHANGELOG.md.
 */
export const APP_VERSION = "0.8.0";

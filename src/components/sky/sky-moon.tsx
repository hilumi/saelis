/**
 * A quiet pearl disc, softly misted — not photorealistic, not an icon, and no
 * lunar-phase calculation in this milestone.
 */
export function SkyMoon() {
  return <div aria-hidden="true" data-testid="sky-moon" className="sky-moon" />;
}

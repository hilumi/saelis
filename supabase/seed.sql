-- Saelis — development seed
-- Intentionally minimal: user-owned tables are populated per-user by triggers
-- when auth users are created. No shared reference data exists in Phase 1.
-- Add development fixtures here as needed. Never seed real user data.

select 1;
